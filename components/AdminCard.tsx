import { Card } from "./ui/Card";

export default function AdminCard() {
    return (
        <Card
            title="Admin Portal"
            description="View dynamic pricing logs, revenue analytics, and system health."
            action="Open Portal"
        />
    );
}
